"use client";

import { Box, styled } from "@mui/material";

const Display = () => {
  return (
    <Box
      sx={{
        justifyContent: "center",
        alignItems: "center",
        display: "flex",
        height: "100vh",
        color: "black",
        fontSize: "32px",
      }}
    >
      Coming Soon
    </Box>
  );
};

export default Display;
